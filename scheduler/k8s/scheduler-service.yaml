apiVersion: v1
kind: Service
metadata:
  name: gpu-scheduler-webhook
  namespace: gpu-scheduler
  labels:
    app: gpu-scheduler
spec:
  selector:
    app: gpu-scheduler
  type: ClusterIP
  ports:
    - name: https
      port: 443
      targetPort: 8443
      protocol: TCP
  