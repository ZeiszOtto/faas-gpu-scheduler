apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: face-detect
  namespace: default
spec:
  template:
    spec:
      containers:
        - image: zeiszotto/face-detect:latest
          ports:
            - containerPort: 8080
          resources:
            limits:
              nvidia.com/gpu: "1"